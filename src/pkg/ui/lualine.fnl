(lambda lualine-config []
  {:lazy false
   :opts {:options {:icons_enabled true
                    :theme :auto}}
                    
   :depdendencies [:nvim-tree/nvim-web-devicons]})

(lualine-config)
